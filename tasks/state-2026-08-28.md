# Session State: oci-labs - 2026-08-28

Supersedes `state-2026-08-24.md`. Steuerdokument bleibt
`tasks/roadmap-cpu-lab.md`; M3 hat einen eigenen Plan.

## Done this session

- **M2-Kern angewendet.** `envs/core` in `trivadisbdsxsp` als `chzh-l-core-01`:
  20 Ressourcen, Re-Plan ohne Drift, `core_owner = "core"` auf allen 19
  OCI-Ressourcen. Kein Bucket, keine Compute. **Noch kein Konsument** -
  `cpu-patch-test` besitzt weiter sein eigenes Netz auf demselben CIDR in
  eigener VCN, unangetastet solange die DB dort lebt.
- **M1 auf `main`.** Der Konverter-Commit `d56b1fe` war bereits gepusht, auf
  `feat/report-redesign-schema-v5`. `cpu-patch-tests/main` per Fast-Forward auf
  `20c722f`, 30 Commits, ohne Merge-Commit und ohne Checkout - der unfertige
  Arbeitsstand dort blieb unberuehrt.
- **E2 entschieden (B) und am selben Tag korrigiert.** Die
  `odb_autoupgrade`-Haelfte ist gestrichen: das Lab nutzt plain AutoUpgrade,
  laedt `autoupgrade.jar` direkt von Oracle. Kein Verweis auf das Repo im ganzen
  Lab. M3 schrumpfte damit auf einen Change Request, **M4 ist nicht mehr
  blockiert.**
- **JDK-Abweichung geklaert.** `39329591` ist der im Juli-CPU publizierte
  JDK-Patch, `39791916` der am Lauftag aktuelle Bundle Patch; das
  AutoUpgrade-Keyword `JDK` nimmt keine Patch-Nummer. Der Defekt liegt im
  Report, der `39329591` als getestet fuehrt. Entschieden: der Report sagt, was
  getestet wurde. Lab-seitig ist nichts zu bauen -
  `measured.home_patches` traegt die installierten IDs bereits.
- **`INJECT_FACTS_AS_VARS` geschlossen.** Zwoelf Referenzen in acht Dateien,
  nicht 82 - der hohe Zaehlwert kam vom vendorten Collections-Baum und von
  Connection-Variablen. Alles auf `ansible_facts[...]`,
  `inject_facts_as_vars = False` gepinnt. Verifiziert per Offline-Rendering,
  `--syntax-check` ueber sieben Playbooks, `make lint`.
- **Defekt dabei gefunden und behoben:** Report-Stem und Gold-Image-Name haben
  keinen Fallback und rendern ohne gesammelte Facts **ohne Datum** - der naechste
  Lauf ueberschreibt das Beweismaterial des vorigen unter identischem Namen.
  `assert_vars.yml` prueft den Fact jetzt.
- **`assign_public_ip` Default auf `false`** in `envs/cpu-patch-test` und
  `modules/oracle_db_host`. Der Schalter waehlt zusaetzlich Subnetz und
  Inventory-IPs - unter dem Default braucht jedes Ansible-Ziel `BASTION=1`.
- **Lint-Schulden geschlossen**, `make lint` repo-weit gruen. CHANGELOG
  entdoppelt: 365 Zeilen v0.3.0-Arbeit lagen unter einer zweiten
  `Unreleased`-Sektion, 393 Inhaltszeilen rein und raus als identische
  Multimenge.
- **Change Request an oradba gestellt**, `tasks/cr-oradba-shared-templates.md` -
  kein Edit dort, eine eigene Session arbeitet mit 21 geaenderten Dateien auf
  `fix/boot-path-gates`.

## Open (P1)

- [ ] Patch-Lauf auf 19.32. **Blockiert:** `op` ist nicht angemeldet, MOS-Credentials
  sind unerreichbar. Braucht `! eval $(op signin)` in einer interaktiven Sitzung.
  Beweist den B2-Fix (`oratab` zurueck auf `:Y`) - der einzige der sechs Befunde,
  der geschrieben aber nie ausgefuehrt wurde.
- [ ] E4 entscheiden: Gold-Image-Test jetzt (Empfehlung A, zweite Instanz mit
  `lab_count=2`) oder weiter vertagen. Die 2-Minuten-Behauptung fuer den
  Gold-Image-Install ist unbelegt.

## Open (P2)

- [ ] M4 (26ai) starten - frei, nichts blockiert es mehr.
- [ ] `cpu-patch-test` auf den Core migrieren, Rest von M2. Wartet auf den
  Patch-Lauf, weil die DB und der Restore Point noch gebraucht werden.
- [ ] Verifikations-SQL aus `verify.yml`/`smoke.yml` extrahieren - liegt heute
  inline, keine Datei.

## Zu melden, nicht hier zu tun

- **Report-Session:** `tested_patches` aus `measured.home_patches` speisen. Und
  der Patch-Listen-Widerspruch - `patches/au/*.cfg` laedt explizit
  `RU:19.32,OJVM,OPATCH,DPBP,JDK`, das Lab testet mit `RECOMMENDED:19.32,JDK`;
  dazu absolute Pfade auf ein Home in diesen Dateien.
- **oradba-Session:** der Change Request, drei Fragen darin.

## To resume

```bash
cd ~/Repos/own/oehrlis/oci-labs
sed -n '1,60p' tasks/state-2026-08-28.md
eval $(op signin)                      # nur fuer den Patch-Lauf
make cpu-lab-start                     # Instanz hochfahren, ca. 1 min
make cpu-lab-progress BASTION=1        # Tunnel wird selbst aufgebaut
```

Die Instanz ist **STOPPED, nicht zerstoert** (gegen OCI geprueft). Sie hat ihre
ephemere Public IP freigegeben - deshalb zeigt der Plan von `cpu-patch-test`
eine Ersetzung von `local_file.ansible_inventory`. Das loest sich beim Start mit
einer neuen Adresse.

<!-- EOF -->
