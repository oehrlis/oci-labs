# Session State - 2026-08-24

Kurzkarte zum Wiedereinstieg. Details stehen in
`tasks/state-cpu-lab-2026-08-23.md`, Plan und Entscheide in
`tasks/roadmap-cpu-lab.md`.

## Erledigt

- **M0 abgeschlossen, `v0.3.0` getaggt und gepusht.** Rollback- und Reboot-Test
  bewiesen, `make lint` repo-weit gruen (Terraform, Ansible, YAML, Markdown,
  Shell).
- **Reboot-Test gruen im dritten Anlauf.** 17 Sekunden von der systemd-Unit bis
  zur offenen Datenbank, unbeaufsichtigt, ohne geladenes Profil.
- **Sechs oradba-Defekte gefunden und behoben**, alle vorbestehend in v1.0.0,
  alle derselben Klasse. Releases 1.0.1 bis 1.0.4.
- **Alle sechs Lab-Befunde geschlossen** (B1 bis B6). B1, B4 und B6 auf dem Host
  bewiesen, B5 dokumentiert, B2 geschrieben aber nicht ausgefuehrt.
- **M1 fertig**: `import_lab_results.py` plus `make import-lab` in
  `cpu-patch-tests`. Committet, **nicht gepusht**.
- **M2 teilweise**: `modules/core` und `envs/core` gebaut, validiert, gegen die
  Tenancy geplant. **Nicht angewendet.**
- Zwei Dokumente fuer den Review: `tasks/roadmap-cpu-lab.md` und
  `~/Repos/own/oehrlis/oradba/tasks/review-brief-boot-path-2026-08-23.md`.

## Offen - P1

1. **`eval $(op signin)`**, dann Patch-Lauf auf 19.32. Beweist den B2-Fix
   (`oratab` zurueck auf `:Y`) und bringt das Lab in den getesteten
   Zielzustand. Ohne 1Password kein Zugriff auf die MOS-Zugangsdaten.
2. **E2 entscheiden** - wo die geteilten Assets leben. Blockiert M3 und damit
   M4 (26ai). Konflikt: oradba ist die *interaktive* Toolbox, die
   AutoUpgrade-Assets sind *Automations*-Assets.
3. **JDK-Abweichung klaeren** - Periodendaten sagen `39329591`, das Lab
   installiert `39791916`. Eines von beiden ist falsch.
4. **`cpu-patch-tests` pushen oder nicht** - der M1-Commit liegt dort lokal,
   neben unfertigen Aenderungen an `VERSION` und `data/2026-07.yaml`.

## Wiedereinstieg

Das Lab ist gestoppt, nicht zerstoert.

```bash
eval $(op signin)                     # nur fuer Patch-Laeufe noetig
make cpu-lab-start                    # Instanz hochfahren, ca. 1 min
make cpu-lab-progress BASTION=1       # Tunnel wird selbst aufgebaut
```

`BASTION=1` ersetzt das fruehere `ANSIBLE_EXTRA`-Gefummel: es setzt Host und
Port und macht den Tunnel zur Vorbedingung, mit Wiederholung und
Session-Neuanlage.
